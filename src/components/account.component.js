import { Typography, Grid } from "@material-ui/core";

export default function (props) {

    return (
        <>
            <Grid container direction="column" style={{ paddingTop: '3em' }}justify="center">
                <Typography variant="h4">Account Details Page Here! </Typography>
            </Grid>
        </>
    );
}